bootJar {
    enabled = false
}

jar {
    enabled = true
}

dependencies {
    testImplementation project(path: ':product')
    testImplementation project(path: ':order')
    testImplementation project(path: ':point')
}
